#include<stdio.h>
#include<conio.h>

void main()
{
 int i,j,n,first,sec,a[10],fre[10],count=1;

  clrscr();
  printf("enter the no of values:");
  scanf("%d",&n);

  printf("\nenter the values:\n");
  for(i=0;i<n;i++)
  scanf("%d",&a[i]);

  printf("value of array:\n");
  for(i=0;i<n;i++)
  printf("%d\t",a[i]);

  fre[i]=-1;
 // printf("%d\t",fre[i]);
  for(i=0;i<n;i++)
   for(j=i+1;j<n;j++)
     {
     if(a[i]==a[i])
       count++;
       fre[j]=0;
      fre[i]=count;
      }
      for(i=0;i<n;i++)
      printf("\n frequency of %d is %d",a[i],fre[i]);

  getch();
  }
